{
  "name": "astro-freelance-persona-monorepo",
  "private": true,
  "workspaces": [
    "theme",
    "playground"
  ],
  "scripts": {
    "dev": "bun --filter my-freelance-portfolio dev",
    "build": "bun --filter theme build",
    "test": "bun --filter theme test"
  }
}