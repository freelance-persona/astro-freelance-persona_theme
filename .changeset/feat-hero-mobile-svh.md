---
"astro-freelance-persona_theme": patch
---

feat: hero uses small-viewport height (100svh). On phones 100vh includes the area under the browser URL bar, so bottom-anchored hero content starts below the fold and only appears after scrolling collapses the bar. svh is always the visible area — identical to vh on desktop.
